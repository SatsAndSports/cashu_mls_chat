# Push Server Configuration

# Port to listen on
PORT=3000

# VAPID keys (generate with: npm run generate-vapid)
VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_SUBJECT=mailto:your-email@example.com

# Logging level: 'silent', 'error', 'info', 'debug'
# silent = no logs
# error  = only errors (subscription failures, relay errors)
# info   = important operations (subscriptions, relay connections) - default
# debug  = verbose (all messages, events, detailed processing)
LOG_LEVEL=info

# Note: Nostr relays are provided by clients when they subscribe
# No need to configure relays here
